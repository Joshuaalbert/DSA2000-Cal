version: "3"
services:
  inspect:
    image: inspect
    container_name: inspect
    build:
      context: .
      dockerfile: services/inspect/Dockerfile
    ports:
      - "8888:8890" # Jupyter notebook is nice to have there
    env_file:
      - .env
    volumes:
      - /dev/shm:/dev/shm
      - $RUN_DIR_HOST:/dsa/notebooks/output
      - $DATA_DIR_HOST:/dsa/notebooks/data
    networks:
      - safe-tier
  quartical:
    image: quartical
    container_name: quartical
    build:
      context: .
      dockerfile: services/quartical/Dockerfile
    env_file:
      - .env
    volumes:
      - /dev/shm:/dev/shm
      - $RUN_DIR_HOST:/dsa/run
      - $DATA_DIR_HOST:/dsa/data
      - $PWD/$INPUT_DIR_HOST:/dsa/input # relative path, hence we add $PWD, else should be absolution.
    networks:
      - safe-tier
networks:
  safe-tier:

